/*
import {Action} from "@reduxjs/toolkit";

interface Register {
    name: string
    surname: string
    cellNumber: string
    mail: string
    password: string
}

const initialState = {
    name: "",
    surname: "",
    cellNumber: "",
    mail: "",
    password: "",
} as Register

export default function registerReducer(state:Register=initialState,action:Action){
   /!* switch (action.type){
        /!*case*!/
    }*!/
}*/
