/*
import {UnknownAction} from "@reduxjs/toolkit";


interface CounterState {
    count: number
}

const initialState = {
    count: 0
} as CounterState

export default function counterReducer(state = initialState, action: UnknownAction) {
    switch (action.type) {
        case INCREMENT:
            return {
                ...state,
                count: state.count + 1
            }
        case DECREMENT:
            return {
                ...state,
                count: state.count - 1
            }

        default:
            return {
                ...state,
            };
    }
}
*/



